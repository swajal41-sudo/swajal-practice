import math

def r_area(length ,breadth):
    a = length * breadth
    return a
def c_area(r):
    a = math.pi * (r*2)
    return a
def t_area(height , width ):
    a = (height*width)/2
    return a